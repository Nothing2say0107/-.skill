# 更新日志 (Changelog)

本仓库遵循[语义化版本](https://semver.org/lang/zh-CN/)规范管理 `lelouch` 技能：

| 版本位 | 何时升级 | 示例 |
|--------|----------|------|
| **主版本** (MAJOR) | 修改触发边界、删除步骤、改变输出契约等**不兼容改动** | 修改 `description` 的激活条件、删除「绝对规则」条目 |
| **次版本** (MINOR) | **新增兼容能力**，不影响现有功能 | 新增心智模型、新增触发词、新增参考文件 |
| **修订号** (PATCH) | 错别字、格式等**微小修正** | 修正 README 断链、错字 |

版本管理遵循「源代码层 + 运行时层」双层架构：

- **源代码层**：Git 管理全部变更，每次修改须有提交记录与变更说明；
- **运行时层**：每次发布为不可变版本，开发环境可用 `latest`，**生产环境必须锁定明确版本**（如 `lelouch@1.1.0`），禁止永远跟随 `latest`。

---

## [1.1.0] - 2026-08-15

依据《技术分享：Agent-Skill 版本管理》规范对仓库进行整体规范化。**未修改触发边界、未删除任何步骤、未改变输出契约**，属兼容性新增，升次版本。

### 新增

- **语义化版本元数据**：`SKILL.md` frontmatter 增加 `version: 1.1.0` 字段，运行时可锁定具体版本。
- **版本管理文档**：
  - `CHANGELOG.md`（本文件）：版本记录与升级规则；
  - `SKILL-REGISTRY.md`：技能登记表，记录负责人、代码仓库、当前版本、依赖、评测结果与发布状态；
  - `RELEASE-CHECKLIST.md`：发布验证清单，覆盖触发 / 执行 / 共存 / 安全四维度测试、灰度发布与回滚窗口流程。
- **README 安装章节**：补充安装方法与版本锁定指引，修复原 `[安装](#安装)` 锚点断链。

### 修正

- README 中「7 个调研文件」更正为 **6 个**（`references/research/` 实际存在 01、03、04、05、06、07 共 6 个文件，编号 02 缺失，已在路线图中标记为待补）。
- `lelouch/README.md` 与根 `README.md` 内容同步：补齐此前缺失的免责声明（含侵权删除 / Takedown 条款）与版本信息，消除双 README 漂移隐患。
- `lelouch/LICENSE` 版权行与根 `LICENSE` 统一（补 `Xinghao_Wang`），补充文件末尾换行。

### 不变（兼容性承诺）

- `SKILL.md` 的 `description`（触发边界）逐字未动；
- 5 个核心心智模型、6 条决策启发式、表达 DNA、情感触发词表、绝对规则全部保留；
- `examples/`、`references/research/` 内容未改动。

---

## [1.0.0] - 2026-05-10

初始稳定基线（追溯确立，对应提交 `6b09829`）。

### 内容

- 完整数字人格技能 `SKILL.md`：身份卡、5 个核心心智模型、6 条决策启发式、表达 DNA、7 类情感触发反应、故事时间线、价值观与反模式、智识谱系、诚实边界。
- 6 份调研文件（`references/research/`）：角色档案、性格核心、表达 DNA、决策日志、人际关系、故事弧线。
- 5 段实战示例对话（`examples/demo-conversation.md`）。
- MIT 许可证与非商业二创免责声明（含侵权删除条款）。
