# 发布验证清单 (Release Checklist)

> 新版本上传后**先进入测试环境**，通过四维度验证 → 灰度放量 → 全量发布。
> 单独测试一个 Skill 没问题，不代表放进多 Skill 环境仍然没问题——**共存测试不可省略**。

## 一、四维度测试

### 1. 触发测试（改 `description` 时必做）

- [ ] 激活关键词逐项验证：`鲁路修` / `Lelouch` / `Zero` / `Code Geass` / `黑色王子` / `数字鲁路修`
- [ ] 用户想"与角色聊天 / 扮演互动 / 切换人设"时能正确激活
- [ ] 无关话题（其他动漫角色、普通问答）**不会**误触发
- [ ] `description` 修改后触发边界与文档声明一致

### 2. 执行测试（改正文步骤时必做）

- [ ] 指令遵循回归：绝对规则 5 条（用"我"说话 / 中文默认 / 不解构自己 / 情感真实 / 不死板）逐条通过
- [ ] 输出质量回归：示例话题（尤菲之死 / 朱雀评价 / C.C. 拌嘴 / 黑骑入团）输出符合表达 DNA
- [ ] 退出机制有效："退出角色"“切回正常”“不用扮演了"可正常退出
- [ ] 诚实边界：对现代概念、现实政治的回应符合设定的局限说明

### 3. 共存测试（多技能环境必做）

- [ ] 与其他**角色人格类** Skill 同时安装时，不互抢"和 XX 聊天"类请求
- [ ] 描述与现有 Skill 无重叠触发词；若重叠，明确优先级或收窄边界
- [ ] 与通用对话 / 写作类 Skill 共存时，仅在被显式点名时接管

### 4. 安全测试（新增脚本或依赖时必做）

- [ ] 本技能为纯 Markdown 提示词资产：无脚本执行、无外部依赖、无权限需求（保持此性质，变更须重新评估）
- [ ] 失败兜底：模型未进入角色时，用户有明确的退出与纠错路径
- [ ] 内容边界：不生成违反平台规范的内容；Geass 仅作虚构设定讨论，不用于"控制用户"话术

## 二、灰度发布

- [ ] 新版本先路由**小部分流量**（如自用环境 / 测试会话）验证
- [ ] 确认正常后逐步扩大范围，异常时**直接切回旧版本**
- [ ] 灰度期间登记表（`SKILL-REGISTRY.md`）记录评测结果

## 三、回滚保障

- [ ] 发布前旧版本保留**可回滚窗口**（旧版本不立即删除，生产清单保留上一稳定版）
- [ ] Git 对应版本打 tag（如 `v1.1.0`），确保可精确回到任意历史版本
- [ ] 回滚后旧版本保持"停止新流量、观察存量调用"状态，确认稳定再清理

## 四、发布动作

- [ ] `SKILL.md` frontmatter `version` 已更新
- [ ] `CHANGELOG.md` 新增版本条目（含变更说明与类型判定）
- [ ] `SKILL-REGISTRY.md` 版本履历与评测记录已同步
- [ ] 提交信息含变更说明（建议格式：`type(scope): 摘要`，正文写明改了什么、为什么）
- [ ] 生产环境锁定新版本号，而非 `latest`

---

*清单模板依据《Agent-Skill 版本管理》最佳实践：触发 / 执行 / 共存 / 安全四维测试 + 灰度发布 + 回滚窗口。*
